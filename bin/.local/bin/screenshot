#!/usr/bin/env bash

OUTPUT='/home/mitchthorson/Pictures/screenshots/%Y-%m-%d-%T-screenshot.png'

case "$1" in
	"select") scrot "$OUTPUT" --select --line mode=edge || exit;;
	"window") scrot "$OUTPUT" --focused --border || exit;;
	*) scrot "$OUTPUT" || exit;;
esac

notify-send "Screenshot taken."
